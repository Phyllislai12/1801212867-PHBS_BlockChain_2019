//
// Source code recreated from a .class file by IntelliJ IDEA
// (powered by Fernflower decompiler)
//

import Transaction.Output;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.Set;

public class UTXOPool {
    private HashMap<UTXO, Output> H;

    public UTXOPool() {
        this.H = new HashMap();
    }

    public UTXOPool(UTXOPool uPool) {
        this.H = new HashMap(uPool.H);
    }

    public void addUTXO(UTXO utxo, Output txOut) {
        this.H.put(utxo, txOut);
    }

    public void removeUTXO(UTXO utxo) {
        this.H.remove(utxo);
    }

    public Output getTxOutput(UTXO ut) {
        return (Output)this.H.get(ut);
    }

    public boolean contains(UTXO utxo) {
        return this.H.containsKey(utxo);
    }

    public ArrayList<UTXO> getAllUTXO() {
        Set<UTXO> setUTXO = this.H.keySet();
        ArrayList<UTXO> allUTXO = new ArrayList();
        Iterator var4 = setUTXO.iterator();

        while(var4.hasNext()) {
            UTXO ut = (UTXO)var4.next();
            allUTXO.add(ut);
        }

        return allUTXO;
    }
}
